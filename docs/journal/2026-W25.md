# Week 25 회고 (2026-06-15 ~ 21)

> 관리자 요청 2건 착수 — 아이폰 세션 유지 / Twin 닉네임 전역 표시

## 2026-06-16

### 요청 1 — 아이폰 세션 유지 (ADR 선행)
- **현상(측정)**: PC는 로그인 유지, 아이폰 Safari는 며칠 만에 로그아웃.
- **원인(가설)**: 세션이 localStorage 저장(`session.ts`) + 게이트웨이 크로스 오리진 → iOS Safari ITP의 스크립트-저장소 7일 캡에 걸림. 서버 토큰은 30일 유효(`tokens.ts`).
- **ADR 0014 (Proposed)** 작성: 세션을 httpOnly·Secure·SameSite=Lax 쿠키로 전환. 전제 = 게이트웨이를 웹과 동일 site(`api-chronoshealth.ever-day.com`)로 이전.
- **착수 대기**: ① 아이폰 7일+ 재현으로 가설 확정 ② 게이트웨이 도메인 이전 승인.

### 요청 2 — Twin 닉네임 전역 표시 (구현 완료)
- 발견: 닉네임 백엔드(`/me`, check-nickname, lock)·입력 UI(`/twin`)·4언어 i18n 모두 기존 존재. 남은 건 **표시 일원화**.
- `lib/profile-state.ts` 신설 — `useTwinNickname()` 훅(localStorage 캐시, 세션 토큰별 invalidate, admin-state 패턴).
- `session.ts` — 로그인/로그아웃 시 닉네임 캐시 무효화.
- `AppShell` 헤더 — title 없는 화면에 닉네임 칩 상시 표시.
- `UserMenu` 드롭다운 — 로그인 시 닉네임 노출(title 있는 화면 커버).
- `profile` 페이지 — 닉네임 Row 추가. **실명은 마이페이지에서만 유지**(기존 reveal/mask 그대로).
- 검증: `tsc --noEmit` 0 에러, `next build` 0 에러(전 라우트 정적 프리렌더).
- **미반영(후속)**: 커뮤니티 글/댓글 작성자명 닉네임 연동 — 게이트웨이가 IDENTITY_DB 닉네임 조회 필요 + PII 격리(절대규칙 1) 검토 후 별도 슬라이스.
