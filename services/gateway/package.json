{
  "name": "@chronos/gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Chronos Health API Gateway (Hono on Cloudflare Workers)",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir dist",
    "lint": "echo 'lint placeholder — ESLint config 도입 시 활성화'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240903.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "wrangler": "^3.78.0"
  }
}
